I notice that the task description asks me to "Add visualization support for isolated subcircuit sections" but doesn't provide specific file paths or hints about the codebase structure.

However, I can see from the prompt that my working directory is: `{cwd}`

To proceed effectively, I need more specific information about:

1. **What project/codebase is in the working directory?** - Is this an EDA tool, circuit simulator, or something else?
2. **What files are relevant to this task?** - Please provide file paths or globs (e.g., `src/visualization/*.py`, `circuit/*.rs`)
3. **What does "isolated subcircuit sections" mean in this context?** - Is this about:
   - Highlighting selected subcircuits in a schematic viewer?
   - Creating separate visualization windows for subcircuits?
   - Exporting subcircuit sections as images/SVGs?
4. **Are there existing tests or patterns to follow?** - Point me to similar visualization code that already works

Please provide the specific file paths or additional context so I can:
- Read the relevant source files
- Identify existing patterns for visualization
- Create a concrete implementation plan

Without this information, I can only make generic suggestions about how one might add subcircuit visualization support in an EDA or circuit simulation tool.