I need clarification before I can proceed. The task description mentions implementing an SVG circuit schematic renderer, but:

1. **No specific files are mentioned** in the task description
2. **The working directory** is shown as `{cwd}` which appears to be a placeholder that wasn't filled in

Could you please provide:

- The actual working directory path (or list of relevant files)
- Any specific file paths that are involved in this task
- Any existing code or patterns related to "SVG circuit schematic renderer" that already exist in the codebase

Without this information, I cannot read the target files, identify patterns, or produce the requested markdown plan.