Let me search for PDF-related code in the project:
Let me also check for any existing documentation about PDF export:
Now let me run the PDF-related tests to verify they pass:
Now I'll update the lib/README.md to add comprehensive PDF export documentation:
Let me run a final verification of all PDF-related tests:
## Summary

I updated the documentation in `lib/README.md` with comprehensive PDF export usage examples and API reference. Here's what was added:

### Documentation Added

1. **Quick Start** - Basic usage examples showing how to convert markdown to PDF with the simple `export_to_pdf()` function

2. **Dependencies Section** - How to check available PDF backends using `check_dependencies()` and installation instructions for pandoc, weasyprint, and wkhtmltopdf

3. **API Reference** for:
   - `export_to_pdf()` - Complete function signature with parameter table, return type, and exception documentation
   - `PDFBuilder` class - Full builder API with:
     - Configuration methods (`page_size()`, `margins()`, `font_family()`, `font_size()`, `line_height()`, `title()`, `author()`, `header()`, `footer()`, `include_toc()`, `custom_css()`, `base_url()`)
     - Build methods (`build_from_html()`, `build_from_markdown()`, `build_from_file()`)
     - Page sizes constant

4. **5 Usage Examples**:
   - Example 1: Simple markdown to PDF
   - Example 2: Custom layout with builder (full configuration)
   - Example 3: Building from HTML content
   - Example 4: Building from inline markdown
   - Example 5: PDF with email attachment integration

5. **Specialized Export Functions** - Documentation for `export_daily_briefing()` and references to `export_research_run()` / `export_research_report()`

### Verification

- All 28 PDF-related tests pass ✅
- Library imports correctly ✅
- The test failures observed during verification are pre-existing (missing `networkx` dependency) and unrelated to my changes