I'll start by exploring the codebase to understand the existing structure and patterns before implementing the complexity scoring system.
Let me search for any existing complexity-related code and understand the project better:
Let me explore the existing analysis code and understand patterns:
Let me also check the Circuit class to understand its structure:
Let me see what's implemented and what needs to be added:
Let me check what's exported from the analysis module:
Let me check if there are any tests for complexity to understand what's expected:
Now I understand the codebase. I need to:
1. Add a `CalculateComplexityScore()` function as a top-level convenience function
2. Ensure the complexity module is properly exported from `__init__.py`
3. Potentially add a simpler `ComplexityMetrics` struct

Let me implement these changes:
Now I'll add the `CalculateComplexityScore()` function to the complexity.py file. I'll add it at the end:
Now let me add a `ComplexityMetrics` alias and ensure proper exports in `__init__.py`:
Now let's update the analysis `__init__.py` to export the complexity-related items:
The file already has complexity imports but is missing `CalculateComplexityScore` and `ComplexityMetrics`. Let me update it:
Now update the `__all__` list:
Now let's verify everything works by testing imports:
Let's test the full functionality with a circuit:
Let me check what's happening with the weighted_score property:
I see the issue. The `ComplexityScore` class uses `final_score` but the property in `CircuitComplexityMetrics` references `weighted_score`. Let me fix this:
Now let's run the tests to ensure nothing is broken: