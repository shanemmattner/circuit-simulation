Now I understand the problem. Let me implement the isolated subcircuit detection. First, let me check for any existing similar functionality:
Now let me create a test file for the graph module:
Let me check if the graph module exports are correctly set up:
Let me run a broader test to make sure the implementation integrates well:
## Summary

I've implemented isolated subcircuit detection in the circuit simulation library. Here's what was added:

### New Functions in `src/circuit_sim/graph.py`:

1. **`find_nodes_reachable_from_ground(circuit)`** - Performs BFS starting from ground (node 0) to find all nodes electrically connected to the ground reference.

2. **`find_isolated_nodes(circuit)`** - Returns all nodes that are NOT reachable from ground (nodes with no electrical path to ground).

3. **`find_isolated_subcircuits(circuit)`** - Returns connected components (groups of nodes) that are not connected to ground - these represent floating sections of the circuit.

4. **`get_isolation_report(circuit)`** - Provides a comprehensive report with:
   - `reachable_nodes`: Set of nodes connected to ground
   - `isolated_nodes`: Set of isolated nodes
   - `isolated_subcircuits`: List of isolated connected components
   - `is_ground_connected`: Whether ground exists
   - `has_isolated_sections`: Whether there are any floating sections

### Test File: `tests/test_graph.py`

Created 22 tests covering:
- Building adjacency graphs
- Finding connected components
- Finding reachable/isolated nodes
- Finding isolated subcircuits
- Comprehensive isolation reports
- Real-world circuit examples (voltage dividers, RC filters, opamp circuits)

All 22 tests pass successfully.