Let me explore the tests directory and search for any existing graph-related code.
Let me look at the mesh analysis test and also search for any existing graph algorithms in the codebase.
Now let me search for any existing connectivity-related code in the codebase.
Now I have a clear understanding of the codebase. Let me implement the graph connectivity analysis functions in mesh_analysis.py.
Now let me add tests for the connectivity analysis functions. First, let me read the full test file again to see its structure.
Now let's run the tests to verify the implementation:
I need to fix the logic for isolated nodes. The current implementation doesn't correctly identify isolated nets (nets with no connections). Let me fix this:
All tests pass. Let me run a broader test to make sure the implementation doesn't break anything else in the project:
The implementation is complete and working. Here's a summary of what was implemented:

## Summary

### Files Modified

1. **`lib/mesh_analysis.py`** - Added graph connectivity analysis functions:
   - `ConnectivityResult` - NamedTuple to hold connectivity analysis results
   - `find_connected_components()` - Finds all connected components in a circuit graph using NetworkX's connected_components algorithm
   - `get_number_connected_components()` - Returns the count of connected components
   - `is_circuit_connected()` - Checks if the circuit is fully connected (single component with no isolated nodes)
   - `analyze_connectivity()` - Comprehensive analysis returning all connectivity information

2. **`tests/test_mesh_analysis.py`** - Added 16 new tests for connectivity analysis:
   - `TestFindConnectedComponents` - 4 tests
   - `TestGetNumberConnectedComponents` - 3 tests
   - `TestIsCircuitConnected` - 4 tests
   - `TestAnalyzeConnectivity` - 5 tests

### Implementation Details

The connectivity analysis uses NetworkX's built-in connected components algorithm:
- `nx.connected_components()` - finds all connected components
- `nx.number_connected_components()` - counts components
- `nx.is_connected()` - checks if graph is fully connected

The implementation handles edge cases:
- Empty circuits
- Single-component circuits
- Disconnected circuits with multiple isolated sub-circuits
- Circuits with isolated nets (nets with no connections)

All 31 tests pass (15 existing + 16 new).